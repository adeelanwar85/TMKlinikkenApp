const axios = require('axios'); const API_KEY = '87656ea5-40bc-45bc-87b0-b236678bdfab'; const client = axios.create({ baseURL: 'https://api.bestille.no/v2', headers: { 'Authorization': 'Bearer ' + API_KEY, 'Accept': 'application/json' } }); async function run() { try { console.log('Checking connection /Service...'); const s = await client.get('/Service'); console.log('Services status:', s.status, 'Count:', s.data.length); console.log('Probing Activities (Random IDs)...'); for(let i=12000; i<12010; i++) { try { const res = await client.get('/Activity/'+i); console.log('HIT ID:', i); console.log(JSON.stringify(res.data, null, 2)); break; } catch(e) { if(e.response && e.response.status !== 404) console.log('ID', i, e.response.status); } } } catch (e) { console.error('Error:', e.message); } } run();
